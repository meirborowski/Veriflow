# PostgreSQL
POSTGRES_USER=veriflow
POSTGRES_PASSWORD=veriflow_dev
POSTGRES_DB=veriflow

# Server
DATABASE_URL=postgresql://veriflow:veriflow_dev@db:5432/veriflow
PORT=3001
CLIENT_URL=http://localhost:3000

# Auth (Phase 1)
JWT_SECRET=change-me-jwt-secret
JWT_REFRESH_SECRET=change-me-jwt-refresh-secret

# MinIO (S3-compatible object storage)
MINIO_ROOT_USER=veriflow
MINIO_ROOT_PASSWORD=veriflow_dev
MINIO_ENDPOINT=http://minio:9000
MINIO_PUBLIC_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=veriflow
MINIO_SECRET_KEY=veriflow_dev
MINIO_BUCKET=veriflow-attachments
MINIO_USE_SSL=false

# Automation (Phase 6)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=change-me-32-byte-hex-encryption-key-64-chars
WORKER_API_KEY=change-me-worker-api-key

# Run Spawner (Phase 6 — pod-per-run)
# SPAWNER_TYPE: 'docker' (dev) or 'k8s' (prod)
SPAWNER_TYPE=docker
RUNNER_IMAGE=veriflow-runner:latest
DOCKER_SOCKET=/var/run/docker.sock
DOCKER_NETWORK=veriflow
# Pre-populates the base URL field in the trigger dialog.
# In Docker Compose this should be the client service name so the runner container
# can resolve it via the Docker network.
NEXT_PUBLIC_RUNNER_BASE_URL=http://client:3000

# Kubernetes spawner (only required when SPAWNER_TYPE=k8s)
K8S_NAMESPACE=veriflow
K8S_RUNNER_IMAGE=registry.example.com/veriflow-runner:latest
K8S_SERVICE_ACCOUNT=veriflow-runner
K8S_JOB_TTL_SECONDS=300

# Runner pod env vars (injected at spawn time — set these on the server)
VERIFLOW_API_URL=http://server:3001/api/v1
MAX_RUN_DURATION_MS=600000
